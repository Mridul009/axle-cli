from __future__ import annotations

import os
import shutil
import subprocess
from pathlib import Path

from .models import GooseRunResult, SavedConfig, Workspace
from .repo import changed_files, diff_text
from .terminal import Terminal


# class GooseError(RuntimeError):
#     pass


# def _goose_env(config: SavedConfig) -> dict[str, str]:
    


# def build_command(config: SavedConfig, prompt_text: str, *, model: str | None = None, provider: str | None = None) -> list[str]:
    


# def run_goose
